{
  "name": "gru953-studio-repo-tools",
  "private": true,
  "description": "Dev-only repo tooling (lint/format) for GRU953-Studio. Not shipped inside the plugin \u2014 see CONTRIBUTING.md's 'A note on dependencies'.",
  "scripts": {
    "lint": "eslint plugins/gru953-studio/hooks tools",
    "format:check": "prettier --check \"plugins/gru953-studio/hooks/*.mjs\" \"tools/**/*.mjs\" eslint.config.mjs",
    "build:assets": "node tools/build-release-assets.mjs"
  },
  "devDependencies": {
    "eslint": "^10.8.1",
    "prettier": "^3.9.6"
  }
}
