# Session Findings — Health, Functionality, Bugfix, Optimisation & Upgrade

Companion register to
`1785410295000-session-plan-2026-08-health-functionality-upgrade.md`. Written to before any
finding exists, per that plan's §5 instruction.

## Findings

| # | Sitting | What broke | Reproduction | Severity | Status |
|---|---|---|---|---|---|
| 1 | 1 (W1.1) | Not a defect — a real behaviour worth recording. The owner's first live answer to Q1 gave Standard Tier, not the plan's predicted Tiny. The studio correctly showed the resulting Tier in plain English and let the owner reconsider, exactly as `studio/SKILL.md` documents ("let them raise or lower it at any time") — the owner then changed their answer and Tiny was confirmed. Proves the Tier-override mechanism genuinely works, live, not just in prose. | Live interview, 2026-07-30: Q1 Yes→No (revised), Q2=No, Q3=None, Q4="a computer"→Mac | Informational | **Resolved** — Tiny confirmed, proceeding |
| 2 | 1 (W1.1) | **Real defect.** `traceability-check.mjs` has zero Tier-awareness — the moment `Dev-Memory/` exists, it unconditionally requires `Dev-Memory/REQUIREMENTS.md` or BLOCKs (`isDirectory(devMemory)` is the only gate before the file-existence check; verified by reading the full source, lines 153-183). But `focus-guard/SKILL.md` lines 134-138 explicitly document that "on Tiny Tier the matrix may be a short inline list rather than a full REQUIREMENTS.md table... from Standard Tier up, REQUIREMENTS.md is a file and the traceability check runs at every stage boundary" — i.e. the check is documented as a Standard/Complex-Tier-only gate, but the code enforces it on every Tier including Tiny. Every genuinely Tiny-Tier project that never creates a `REQUIREMENTS.md` (as the documentation says it needn't) would fail this gate at every checkpoint/Publish attempt. | `node plugins/gru953-studio/hooks/traceability-check.mjs ~/gru953-studio-w1-scratch/w1-tiny` → `{"status":"BLOCKED",...}`, 2026-07-30, on a genuine Tiny-Tier project with no REQUIREMENTS.md by design | Medium — would block every real Tiny-Tier project's first checkpoint/Publish attempt, though a workaround (create a minimal REQUIREMENTS.md anyway) exists | **Fixed, sitting 4** — `readTier()` added to `traceability-check.mjs`, reading a new mandated `**Tier:** Tiny/Standard/Complex` line in `OBJECTIVE.md`; Tiny with no REQUIREMENTS.md now reports clean, Standard/Complex unchanged (still BLOCKS). See findings #17-19 below for two defects this itself introduced, also fixed. |
| 3 | 1 (W1.1) | Not a defect, but a real usability rough edge worth noting for W2. `verify-progress.mjs`'s accepted evidence phrasings (`exit 0`, `machine checks true`, `user PASS`) are documented only in a source-code comment, not in `dev-memory/SKILL.md`'s own file-format table. A human-observed approval genuinely needs the literal substring "user PASS" — writing natural evidence like "owner approved live, no changes requested" (without that exact phrase) fails closed with a message that doesn't explain the required phrasing. First attempt at writing this row failed for exactly this reason; corrected once the source comment was found. | `node plugins/gru953-studio/hooks/verify-progress.mjs ~/gru953-studio-w1-scratch/w1-tiny` on the uncorrected row → `{"status":"BLOCKED",...}`, then clean after adding the literal phrase "user PASS", 2026-07-30 | Low | **Fixed, sitting 4** — `dev-memory/SKILL.md`'s PROGRESS.md file-format table now quotes the exact accepted phrasing (`verified: ... user PASS`) for a human-observed approval, sourced verbatim from the hook's own comment. |
| 4 | 2 (W1.2b) | **Real gap.** `lang-swift/SKILL.md` states plainly "Tests use XCTest" and gives `swift test` as the command, with no check of whether XCTest is actually available and no fallback. On a Mac with only Command Line Tools installed (no full Xcode.app — a common, ordinary setup, not a misconfiguration) neither `XCTest` nor Swift's newer `Testing` module can be imported at all: `swift test` fails outright with "no such module". Nothing in `lang-swift` or `architect.md`'s platform → stack map checks this before committing to Swift, and nothing tells the owner they'd need to install the ~7GB full Xcode app just to run tests on their own machine. | On this machine: `xcode-select -p` → `/Library/Developer/CommandLineTools` (no Xcode.app). `swift test` on a package importing `XCTest` → `error: no such module 'XCTest'`. Same result importing `Testing`. 2026-07-30 | Medium — blocks TDD entirely for any Swift project on a Command-Line-Tools-only Mac, with no warning given before the stack is chosen | **Fixed, sitting 4** — `lang-swift/SKILL.md` now carries a flagged caveat naming the exact `xcode-select -p` check and what each result means. |
| 5 | 2 (W1.2b) | **Real mechanism limitation, worth documenting rather than fixing.** The orchestrating agent's own git-worktree isolation feature is bound to the *session's* fixed root directory, not to wherever a command has `cd`'d to — it cannot create a worktree inside a *different* git repository (the scratch project) than the one the session started in. Attempting it fails outright: "Cannot create agent worktree: not in a git repository". Worked around by creating the two worktrees manually with `git worktree add` and dispatching plain (non-isolated) builder agents pointed at each path explicitly — which worked perfectly and is arguably how a real multi-session/multi-terminal setup would look anyway. Recorded so a future session doesn't waste time assuming the built-in isolation flag will work for an out-of-session project. | `Agent(isolation: 'worktree')` pointed at `~/gru953-studio-w1-scratch/w1-standard` from a session rooted at the GRU953-Studio repo → immediate tool error, 2026-07-30. Manual `git worktree add` from the same session succeeded immediately after. | Low — full workaround exists and was used | Resolved — documented, not a defect to fix |
| 6 | 2 (W1.2b) | Not a defect in the studio itself, but a real process gap in this specific test run: neither builder's own instructions, nor mine, mentioned a `.gitignore` for the new Python project (a Swift-oriented one existed from before the stack change, but never got a Python equivalent). Both parallel builders' test runs generated `__pycache__` bytecode files that got committed alongside real source, causing a genuine (if trivial) merge conflict — the only conflict in the whole two-builder merge, and confined entirely to auto-generated binary cache files, never real code. Fixed by untracking `__pycache__`/`*.pyc`/`.pytest_cache/` and re-committing. Worth a one-line addition to `lang-python`'s own skill pack: scaffold a Python `.gitignore` as a matter of course, the same way a Swift/Node project would get one. | `git merge task-3-toggle-bought` → 3 `CONFLICT (content)` lines, all `*.pyc` paths, zero in real source files, 2026-07-30 | Low | **Fixed, sitting 4** — `lang-python/SKILL.md` now mandates scaffolding a `.gitignore` on every new Python project. |
| 7 | 3 (W1.3) | **Real defect, caught by the resume test itself.** `Dev-Memory/` was never added to the scratch project's `.gitignore` and has been committed into its git history since the very first commit — directly against `dev-memory/SKILL.md`'s own explicit rule: "Add `Dev-Memory/` to the project's `.gitignore` the moment the folder is [created]." This is a defect in how this test session ran the studio, not in the studio's own design (the rule is documented clearly) — but it reveals something real underneath: **no hook mechanically checks this.** `scan.mjs` checks that *secrets inside* a gitignored file aren't force-pushed; nothing checks that `Dev-Memory/` itself is actually listed in `.gitignore` before a commit or push. The rule is currently prose-only, enforced purely by the acting agent remembering — which just failed, in this very test. | A genuinely blind subagent, given only the project path and told to resume from memory (no other context), found this on its own during the resume test, 2026-07-30. Confirmed: `git ls-files \| grep Dev-Memory` returns every memory file; `.gitignore` never mentions `Dev-Memory/`. | Medium — a real privacy rule with no mechanical backstop | **Fixed, sitting 4** — `scan.mjs` gained a real deny condition: a push is now blocked if `Dev-Memory/` holds real files and isn't genuinely gitignored (checked via `git check-ignore`, not a hand-rolled matcher). Found and fixed a real cwd-scoping bug in the same file along the way (see finding #20). |
| 8 | 3 (W1.3) | **Real defect.** Phase 1's `QUALITY-GATE.md` marked "Acceptance criteria: pass", but there is no actual way to run the app — only library-level functions and their tests exist, no entry point ties them together. The phased plan's own stated Phase 1 deliverable ("add an item, remove an item, tick items off as bought, and the list is still there next time you open the app") was never genuinely met; the acceptance tick was awarded on the strength of unit tests alone. Same blind subagent also correctly flagged: the "independent code review" tick rested on "the branches merged without conflict", not on anyone actually reading the code for correctness; and "accessibility: pass" was awarded against the mock-up, not the (nonexistent) real product interface. All three ticks in `QUALITY-GATE.md` were premature. | Found by the same blind resume-test subagent, 2026-07-30, purely from reading the committed files — not told anything about how Phase 1 was built. | Medium — a real process gap in how this test session verified its own work, not a studio defect (the quality-gate skill's dimensions are correct; they just weren't honestly checked before being marked pass) | Fixed in the scratch project (see sitting 3 write-up) |
| 9 | 3 (W1.3) | Minor. No `pyproject.toml`/`requirements.txt` declares `pytest` as a dependency — a fresh checkout wouldn't know it needs it. Worth a note for `lang-python`'s skill pack alongside finding #6 (Python-project scaffolding gaps). | Found by the same blind resume-test subagent, 2026-07-30. | Low | **Fixed in the scratch project, and in the studio itself, sitting 4** — `lang-python/SKILL.md` now mandates a `pyproject.toml` declaring dependencies and setting `pythonpath = ["."]`. |
| 10 | 3 | **The most important finding of this whole sitting — escalated to the owner and resolved.** The mock-up the owner approved is a graphical Mac window (text field, Add button, a tick-box and Remove button per row). What actually shipped, as a side effect of the Swift→Python language switch, is a command-line tool with no window at all. The stack-change decision record justified the switch purely in terms of *logic* ("perfectly capable of expressing the same add/remove/persist/toggle-bought logic") — it never surfaced that the *interface* itself would also change, and never put that specific change back to the owner the way the Prototype gate's own revision-loop mechanism (tested successfully in sitting 2) is designed to handle exactly this kind of change. This is precisely the silent-absorption-of-a-real-change this whole project's approval gates exist to prevent, and it happened anyway, mid-test. | Independent `reviewer` pass, 2026-07-30, comparing the shipped `cli.py` against the approved `Dev-Memory/warframe/index.html`. | High — a real change to what was promised, delivered without asking | Owner ruling, 2026-07-30: keep the command-line version for the remainder of this test — it's a rehearsal of the studio's process, not a real product. **Documentation fix landed sitting 4:** `warframe-prototype/SKILL.md` now states explicitly that an implementation-approach change (language/framework/platform) must be checked for whether it also changes the user-facing shape, and if so goes back through the same approval gate — never absorbed silently because the underlying reason was technical. |
| 11 | 3 | Four real, small code bugs found by the same independent review, all fixed: item names weren't trimmed (a padded or blank name became unmatchable or invisible); the CLI claimed success ("Removed: Eggs") even when nothing matched; a corrupted or half-written save file crashed every future command with a raw stack trace instead of a plain message (a real risk, since saving overwrites the file before checking it wrote correctly); adding the same item twice let `remove`/`bought` silently act on both copies. | `reviewer` agent, 2026-07-30, reproduced each by running the CLI directly. | Medium | All four fixed. The duplicate-name question was a genuine scope choice, not a bug — owner ruling, 2026-07-30: reject a duplicate name outright, simplest mental model for a shopping list |
| 12 | 3 | Two documentation-only defects: no README ships with the app at all (the only "docs" cited were the private memory notes, which correctly never ship — so the shipped artefact genuinely has none); and the plain `pytest` command fails outright from the repo root (`ModuleNotFoundError`) — only `python3 -m pytest` works, which the Definition of Done didn't disclose. | `reviewer` agent, 2026-07-30. | Low | Both fixed |
| 13 | 3 | Cosmetic drift between the two parallel builders, visible only once their code sits side by side: one module documents its no-mutation contract explicitly, the other doesn't; one uses `dataclasses.replace`, the other constructs items literally; the test files disagree on how they assert non-mutation. None of this is a bug — it's what genuinely-independent parallel work looks like before a light pass reconciles style. | `reviewer` agent, 2026-07-30. | Low | Open — cosmetic, left for Phase 2's own review pass |
| 14 | 3 | Not a studio defect — my own process gap. `warframe-prototype/SKILL.md` says the Design stage's real deliverable is `ARCHITECTURE.md`; this session only recorded the stack decision loosely in `Dev-Memory/decisions/`, never a proper `ARCHITECTURE.md`. Noticed only while checking `/studio-dashboard`'s rendering (it correctly showed no architecture section, since the file never existed). | Self-noticed while verifying the dashboard render, 2026-07-30. | Low | Open — noted, not corrected retroactively; doesn't affect any result already recorded |
| 15 | 3 | `/studio-dashboard` and `/studio-status` both verified genuinely working, not assumed. `dashboard.mjs` run for real against the scratch project: correctly rendered the objective, the live task board (5/5 done, matching `PROGRESS.md` exactly, verified by grepping the actual `<tr>` rows rather than trusting the "written" status line), and correctly omitted an architecture section since none exists (see finding #14) rather than rendering a broken or empty one. | `node dashboard.mjs ~/gru953-studio-w1-scratch/w1-standard` → `{"status":"written",...}`, then the HTML inspected directly, 2026-07-30. | — | Confirmed working, no defect |
| 16 | 3 | Not a studio defect — my own error, caught by `memory-integrity.mjs` doing exactly its job. Wrote `FOCUS.md`'s Active phase as "Done — both phases complete", which isn't one of the eleven documented lifecycle phases the hook checks against (Brainstorm/Ideate/Design/Prototype/Content/Plan/Build/Test/Fix/Review/Publish/Maintain). Correctly BLOCKED until fixed to "Review" — the last real completed stage, since Publish is deliberately out of scope for this test. Good evidence the hook fails closed on a genuine mismatch rather than just on obviously-malformed input. | `node memory-integrity.mjs ~/gru953-studio-w1-scratch/w1-standard` → BLOCKED naming the exact bad value, then clean after correcting to "Review", 2026-07-30. | Low | Fixed |

| 17 | 4 | **High, cross-batch.** `traceability-check.mjs`'s new `readTier()` (finding #2's fix) called `deEmphasise()` on the captured Tier value, so `**Tier:** ~~Tiny~~` (struck-through) was read as a valid `Tiny`, silently relaxing a gate — exceeding what Ruling 1 authorised (a tightening only, never a loosening). Also (finding #3, same root cause) `.split(/\s+/)[0]` took only the first word, so `**Tier:** Tiny or Standard, still deciding` read as a confident `Tiny` despite being literally ambiguous. | Independent `reviewer` pass, 2026-07-31, end-to-end: `**Tier:** ~~Tiny~~` with no REQUIREMENTS.md → clean (should BLOCK). | High | Fixed — `readTier()` now requires an exact, undecorated, single-word match; anything else is `null` (fails closed) |
| 18 | 4 | **Medium, same function.** A `**Tier:** Tiny` line appearing only inside a fenced code block or an indented example (not as the real recorded value) was read as the genuine record. | Same reviewer pass, 2026-07-31. | Medium | Fixed — `readTier()` now skips fenced (` ``` `/`~~~`) and indented content before matching |
| 19 | 4 | **High, pre-existing, not introduced by any sitting-4 batch, but made more consequential by them.** `lib.mjs`'s `readStdin()` swallowed a transient `EAGAIN` read failure (the real, reproducible race when a hook's Node process starts before its parent has finished writing the payload) and returned `''`, which both `scan.mjs` and `gate.mjs` treated as "no command" → allow. Meant a real `git push` or `gh repo create --public` could silently bypass both security gates under an ordinary timing race, and no prior test could catch it since the whole suite feeds hooks a pre-written buffer, never a genuinely delayed pipe. | Independent `reviewer` pass, 2026-07-31, reproduced the failure class with a mock reader; confirmed `fs.readFileSync(0)` throws real `EAGAIN`/`EISDIR` in the shapes described. | High | Fixed — `readStdinCore()` retries on `EAGAIN`/`EWOULDBLOCK` for a bounded budget, then throws rather than returning `''`; `scan.mjs`/`gate.mjs` now deny on that failure instead of allowing |
| 20 | 4 | **High.** `scan.mjs`'s new Dev-Memory-gitignore check (finding #7's fix) false-positived when the studio root found by walking up the filesystem and the repo actually being pushed were different git work trees (e.g. an unrelated parent folder holding a stray `Dev-Memory/` with no `.git` of its own) — `git check-ignore` exiting 128 (no repo there) was treated the same as exit 1 (genuinely not ignored), denying an entirely innocent push with advice that couldn't fix the reported problem. | Independent `reviewer` pass, 2026-07-31, reproduced exactly: a clean child repo with no `Dev-Memory/` denied because of an unrelated parent folder. | High | Fixed — only runs when `git rev-parse --show-toplevel` agrees between the studio root and the pushed repo; only a genuine exit-1 "not ignored" denies, any other exit code skips the check |
| 21 | 4 | **Medium.** `scan.mjs`'s original secret-file-set scan (`git ls-files` / `git ls-files --others --exclude-standard`, pre-dating this whole session) is cwd-scoped — files elsewhere in the repo, including untracked secret-shaped files, are invisible when the push command's working directory is a subdirectory. The new Dev-Memory check (finding #7/#20) is immune since it addresses `STUDIO_ROOT` directly, but the original file-set scan was not, despite an earlier batch's report claiming this was already fixed. | Independent `reviewer` pass, 2026-07-31, reproduced: files outside the invoking subdirectory absent from both `ls-files` calls; present when run from the root. | Medium | Fixed — both calls now anchored to the repo root via the `:/` magic pathspec |
| 22 | 4 | Low. `lib.mjs`'s new strikethrough/HTML-tag/quote stripping (finding #1's fix) used greedy inner captures, so a cell with two separate decorated spans (e.g. `<b>README</b> and <b>CONTRIBUTING</b>`) could be mangled rather than left alone. | Independent `reviewer` pass, 2026-07-31; confirmed by execution. | Low | Fixed — inner captures tightened to delimiter-excluding character classes |
| 23 | 4 | Low, not a defect. Two tests added for finding #1 pass identically with or without that fix — they guard against the fix being too aggressive (over-stripping ordinary prose), not against the fix being absent, and were at risk of being mistaken for regression coverage. | Independent `reviewer` pass, 2026-07-31. | Low | Fixed — renamed and commented to state plainly what they do and don't prove |
| 24 | 4 | Not a defect — a prior batch's own report overstated what it fixed, claiming a cwd-scoping bug in `scan.mjs`'s file-set scan was resolved when it was not (see finding #21, which is the actual fix). Recorded so a later session doesn't trust a stale "already fixed" claim. | Independent `reviewer` pass, 2026-07-31, cross-checked the claim against the code. | — | Corrected — see finding #21 |

| 25 | 5 | **W3 target not met — recorded honestly, per the plan's own explicit fallback, not fixed.** Fresh baseline (the suite grew from 352 to 391 tests since the plan's original 45.7-49.7s measurement): **50.4-51.2s** across three repeated runs. The plan's target was `≤33s`. Investigated `node:test`'s own concurrency (tried running via `node --test` instead of `node hooks.test.mjs` directly — identical ~51s, no free win) and confirmed the real bottleneck: 372 tests, the large majority making a blocking `spawnSync` call to a fresh `node` subprocess each. Genuine concurrency would need converting the whole suite from synchronous `spawnSync` to `async spawn`+`await`, with every test function marked `async` — a large, invasive rewrite of the safety-critical test coverage for a speed goal, out of proportion to the benefit. | `time node plugins/gru953-studio/hooks/hooks.test.mjs` ×3 → 50.48s/50.99s/51.15s, 2026-07-31; `time node --test <same file>` → 51.26s, no improvement. | — | **Not fixed — measured best recorded, stopped, per plan §7 "if 30% is unreachable without losing coverage, record the measured best and stop."** |
| 26 | 5 | Checked, clean, no action needed. No gate reads the same Dev-Memory file twice within one run (checked all five project-level gates for repeated `read(path.join(...))` call sites on the same path). | Grep sweep of `quality-gate.mjs`, `traceability-check.mjs`, `memory-integrity.mjs`, `content-check.mjs`, `verify-progress.mjs`, 2026-07-31. | — | Confirmed clean, no defect |
| 27 | 5 | W4 upgrades, all verified against fresh sources rather than trusted from the plan's own (unchanged) research: Node 20 dropped from the CI matrix (confirmed EOL 2026-04-30 against `nodejs/Release`'s live schedule); `.nvmrc` moved 22→24; `actions/checkout`'s pinned SHA confirmed to already be exactly `v7.0.1` (only its stale `# v7` comment corrected, in 5 places); `github/codeql-action` re-pinned from `v4.37.3` to the now-current `v4.37.4`. Node 26 deliberately deferred until it reaches long-term support (28 Oct 2026), per the plan's own explicit decision. | `nodejs/Release schedule.json` fetched fresh; `gh api` used to confirm both `actions/checkout`'s and `codeql-action`'s exact current tags/SHAs before editing. 2026-07-31. | — | Fixed |
| 28 | 5 | Checked, clean, no action needed. No skill or agent file hardcodes a specific, potentially-stale Claude model ID — every agent's frontmatter uses a generic tier name (`sonnet`/`opus`/`haiku`), resolved to a real model at runtime by `model-router`, by design. | `grep` sweep across all 35 skills and 38 agent files, 2026-07-31. | — | Confirmed clean, no defect |

| 29 | 6 | W5.2/W5.3 built and verified without needing real accounts. New `.github/workflows/publish.yml`: three independent jobs (npm cli, npm antigravity, VS Code Marketplace), each gated behind its own named GitHub Environment so each publish needs its own separate human approval, using npm Trusted Publishing (no stored npm token). Documented the one-time setup (Environments + reviewers, npm Trusted Publishing registration, `VSCE_PAT` secret) in `CONTRIBUTING.md`. Verified as a real stranger would: packed both npm packages with `npm pack`, installed each from the real tarball into a fully isolated environment (`env -i`, no repo context), and ran the real installed commands — `gru953-studio status` worked correctly both with and without a real project present; `gru953-studio-antigravity` correctly failed with a clear, honest error (not a crash) when run outside a full checkout, exactly matching its already-disclosed limitation. Packaged the VS Code extension into a real `.vsix` and confirmed its contents are exactly the intended files — could not install-and-run it for real, since this machine has no `code` CLI, matching the plan's own already-disclosed limitation. | `npm pack --dry-run` + a real isolated global install for both npm packages; `npx @vscode/vsce package` for the extension. 2026-07-31. | — | Built and verified; not yet live — needs the owner's account setup (npm scope, Azure DevOps publisher, `VSCE_PAT`) and the three GitHub Environments, none of which exist yet |

## Deliberately not done

| # | Date | What was proposed | Who proposed it | Why it is out of scope | Owner decision |
|---|---|---|---|---|---|
